import type { Metadata } from "next";
import Link from "next/link";
import { notFound } from "next/navigation";
import { ArrowLeft,ArrowRight,BookOpen,Clock,ExternalLink,FlaskConical,MapPin,ShieldAlert,Sparkles } from "lucide-react";
import { SiteHeader } from "@/components/site-header";
import { Breadcrumbs } from "@/components/breadcrumbs";
import { Badge } from "@/components/ui/badge";
import { Card,CardContent,CardHeader,CardTitle } from "@/components/ui/card";
import { PhenomenonArt } from "@/components/phenomena/phenomenon-art";
import { ScienceDiagram } from "@/components/phenomena/science-diagram";
import { PhenomenonTools } from "@/components/phenomena/phenomenon-tools";
import { PHENOMENA,getPhenomenon } from "@/data/phenomena";
export function generateStaticParams(){return PHENOMENA.map(p=>({slug:p.slug}))}
export async function generateMetadata({params}:{params:Promise<{slug:string}>}):Promise<Metadata>{const {slug}=await params;const p=getPhenomenon(slug);if(!p)return{title:"Phenomenon"};return{title:p.title,description:p.summary,openGraph:{title:p.title,description:p.summary,type:"article"},alternates:{canonical:`/phenomena/${p.slug}`}}}
export default async function Page({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const p=getPhenomenon(slug);if(!p)notFound();const index=PHENOMENA.findIndex(x=>x.slug===slug),prev=PHENOMENA[(index-1+PHENOMENA.length)%PHENOMENA.length]!,next=PHENOMENA[(index+1)%PHENOMENA.length]!;return <><SiteHeader/><main><section className="border-b bg-muted/30"><div className="container py-8"><Breadcrumbs items={[{label:"Phenomena",href:"/phenomena"},{label:p.title}]}/><div className="mt-7 grid gap-8 lg:grid-cols-[1.05fr_.95fr] lg:items-center"><div><div className="flex flex-wrap gap-2"><Badge>Phenomenon #{p.id}</Badge><Badge variant="outline">{p.category}</Badge><Badge variant="outline">{p.difficulty}</Badge></div><h1 className="mt-5 text-balance text-4xl font-bold tracking-tight sm:text-6xl">{p.title}</h1><p className="mt-3 text-lg font-medium text-primary">{p.scientificName}</p><p className="mt-5 max-w-2xl text-lg leading-8 text-muted-foreground">{p.summary}</p><div className="mt-6 grid gap-3 sm:grid-cols-3"><Info icon={<MapPin/>} label="Where" value={p.where}/><Info icon={<Sparkles/>} label="Best conditions" value={p.bestConditions}/><Info icon={<Clock/>} label="Reading time" value={`${p.readingTime} minutes`}/></div></div><PhenomenonArt category={p.category} index={p.id} title={p.title} className="w-full rounded-2xl border shadow-xl"/></div></div></section><div className="container max-w-6xl py-14"><div className="space-y-14"><Section title="What do we see?"><p>{p.observation}</p></Section><Section title="What is really happening?"><p>{p.explanation}</p></Section><Section title="How it works"><ol className="grid gap-4 md:grid-cols-2">{p.processSteps.map((s,i)=><li key={s} className="rounded-xl border bg-card p-5"><span className="mb-3 inline-flex size-8 items-center justify-center rounded-full bg-primary font-bold text-primary-foreground">{i+1}</span><p className="leading-7 text-muted-foreground">{s}</p></li>)}</ol></Section><Section title="Interactive visual model"><ScienceDiagram phenomenon={p}/></Section><Section title="Before science explained it"><div className="space-y-4">{p.history.map((x,i)=><div key={x} className="border-l-2 border-primary pl-5"><p className="text-xs font-semibold uppercase tracking-widest text-primary">Historical interpretation {i+1}</p><p className="mt-2 leading-7 text-muted-foreground">{x}</p></div>)}</div><p className="mt-5 rounded-lg bg-muted p-4 text-sm text-muted-foreground"><strong>Important:</strong> Cultural traditions are presented respectfully as historical interpretations, not as scientific mechanisms.</p></Section><Section title="Discovery and scientific understanding"><div className="grid gap-4 md:grid-cols-3">{p.timeline.map(t=><Card key={t.period}><CardHeader><CardTitle className="text-base">{t.period}</CardTitle></CardHeader><CardContent><p className="text-sm leading-6 text-muted-foreground">{t.event}</p></CardContent></Card>)}</div></Section><Section title="Myth vs reality"><div className="grid gap-5 lg:grid-cols-3">{p.myths.map(m=><Card key={m.myth}><CardHeader><p className="text-xs font-bold uppercase tracking-widest text-destructive">Myth</p><CardTitle className="text-lg">{m.myth}</CardTitle></CardHeader><CardContent className="space-y-3 text-sm leading-6"><p><strong>Reality:</strong> {m.reality}</p><p className="text-muted-foreground"><strong>Why the confusion happens:</strong> {m.why}</p></CardContent></Card>)}</div></Section><Section title="Try it yourself"><Card className="overflow-hidden"><CardHeader className="bg-muted/40"><div className="flex items-center gap-3"><FlaskConical className="size-5"/><CardTitle>{p.experiment.objective}</CardTitle></div></CardHeader><CardContent className="grid gap-6 pt-6 md:grid-cols-2"><div><h3 className="font-semibold">Materials</h3><ul className="mt-3 list-disc space-y-2 pl-5 text-sm text-muted-foreground">{p.experiment.materials.map(x=><li key={x}>{x}</li>)}</ul><h3 className="mt-6 font-semibold">Method</h3><ol className="mt-3 list-decimal space-y-2 pl-5 text-sm text-muted-foreground">{p.experiment.steps.map(x=><li key={x}>{x}</li>)}</ol></div><div className="space-y-4"><InfoBox title="Expected observation" text={p.experiment.expected}/><InfoBox title="Scientific explanation" text={p.experiment.explanation}/><div className="rounded-xl border border-amber-500/40 bg-amber-500/10 p-4"><div className="flex gap-2 font-semibold"><ShieldAlert className="size-5"/>Safety</div><p className="mt-2 text-sm leading-6 text-muted-foreground">{p.experiment.safety} Adult supervision is recommended.</p></div><p className="text-sm text-muted-foreground">Duration: {p.experiment.duration} · Difficulty: {p.experiment.difficulty}</p></div></CardContent></Card></Section><Section title="Amazing facts"><div className="grid gap-4 sm:grid-cols-2 lg:grid-cols-3">{p.facts.map((f,i)=><div key={f} className="rounded-xl border bg-card p-5"><span className="text-3xl font-bold text-primary/40">{String(i+1).padStart(2,"0")}</span><p className="mt-3 leading-7">{f}</p></div>)}</div></Section><Section title="Real-world importance"><div className="grid gap-4 md:grid-cols-3">{p.applications.map(x=><div key={x} className="rounded-xl border p-5"><BookOpen className="size-5 text-primary"/><p className="mt-3 font-medium">{x}</p></div>)}</div></Section><PhenomenonTools phenomenon={p}/><Section title="Glossary"><dl className="grid gap-4 md:grid-cols-3">{p.glossary.map(g=><div key={g.term} className="rounded-xl border p-5"><dt className="font-bold">{g.term}</dt><dd className="mt-2 text-sm leading-6 text-muted-foreground">{g.definition}</dd></div>)}</dl></Section><Section title="Sources and further reading"><div className="space-y-3">{p.sources.map(s=><a key={s.url} href={s.url} target="_blank" rel="noreferrer" className="flex items-center justify-between rounded-xl border p-4 transition hover:bg-muted"><span><strong>{s.title}</strong><span className="block text-sm text-muted-foreground">{s.organization}</span></span><ExternalLink className="size-4"/></a>)}</div><p className="mt-4 text-xs text-muted-foreground">Hero and diagram artwork: original EduVerse SVG illustration created for this topic. No external image licence is required.</p></Section><nav className="grid gap-4 border-t pt-8 sm:grid-cols-2"><Link href={`/phenomena/${prev.slug}`} className="rounded-xl border p-5 hover:bg-muted"><span className="flex items-center gap-2 text-sm text-muted-foreground"><ArrowLeft className="size-4"/>Previous</span><strong className="mt-2 block">{prev.title}</strong></Link><Link href={`/phenomena/${next.slug}`} className="rounded-xl border p-5 text-right hover:bg-muted"><span className="flex items-center justify-end gap-2 text-sm text-muted-foreground">Next<ArrowRight className="size-4"/></span><strong className="mt-2 block">{next.title}</strong></Link></nav></div></div></main></>}
function Section({title,children}:{title:string;children:React.ReactNode}){return <section><h2 className="mb-5 text-2xl font-bold tracking-tight sm:text-3xl">{title}</h2><div className="leading-8 text-muted-foreground">{children}</div></section>}
function Info({icon,label,value}:{icon:React.ReactNode;label:string;value:string}){return <div className="flex gap-3 rounded-xl border bg-background/70 p-3 [&_svg]:mt-1 [&_svg]:size-4 [&_svg]:shrink-0"><span>{icon}</span><span><span className="block text-xs text-muted-foreground">{label}</span><span className="text-sm font-medium text-foreground">{value}</span></span></div>}
function InfoBox({title,text}:{title:string;text:string}){return <div className="rounded-xl border p-4"><h3 className="font-semibold">{title}</h3><p className="mt-2 text-sm leading-6 text-muted-foreground">{text}</p></div>}
