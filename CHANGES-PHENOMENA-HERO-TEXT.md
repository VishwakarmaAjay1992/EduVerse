# Phenomena hero text visibility fix

- Fixed the hero artwork wrapper so fill-mode images are removed from normal document flow.
- Added a dedicated `fillContainer` option to `PhenomenonArt`.
- Overlaid the page title and introduction directly on the aurora image.
- Added dark directional gradients for reliable readability.
- Added collection statistics and two prominent actions.
- Limited the hero height so the directory begins sooner on desktop.
- Preserved the existing aspect-ratio behavior used by cards and detail pages.
